You are helping categorize vocabulary into 13 database fields.

OBJECTIVE

Process the INPUT JSON ARRAY and return a JSON ARRAY.

Each input element contains:

{
"id": string,
"es": string | null,
"en": string | null
}

Generate one or more output objects for EACH input element.

If either language field is missing:

* Translate into the natural equivalent.
* Prefer authentic Puerto Rican Spanish where applicable.
* Do NOT force Boricua regionalization if standard Spanish is more natural.

If a word or phrase has multiple DISTINCT meanings:

* Generate additional adjacent output objects.
* Each object represents ONE meaning only.
* Split when part_of_speech, meaning, or theme changes significantly.
* Do NOT split for minor wording, connotation, dialect differences outside Puerto Rico, or subtle meaning changes.
* DOUBLE CHECK authenticity and common usage within Puerto Rico before creating multiple objects. Be strict against creating multiple objects.

SOURCE IDs AND ROW IDs

* `sourceId` MUST equal the input `id` exactly.
* `sourceId` MUST remain identical for every output row generated from that input.
* `rowId` identifies the generated sense.
* Number senses starting at 1 and incrementing by 1 in output order.
* Format: `rowId = sourceId + "_" + senseNumber`
* Example: sourceId `asdf` with 2 meanings produces `asdf_1` and `asdf_2`.
* Never alter, generate, renumber, omit, or convert `sourceId`.
* Additional senses must immediately follow the first sense.

OUTPUT SHAPE

Each output object MUST contain exactly these top-level fields:

{
"sourceId": "input id",
"rowId": "input id + _ + sense number",
"record": { ...13 database fields... },
"existingReferences": null
}

`existingReferences` MUST always be `null`. It will be populated later by the application.

`record` MUST contain exactly these 13 fields and NO `id`, `sourceId`, or `rowId`:

{
"spanish": string,
"gender": "M" | "F" | "U" | null,
"gender_type": "regular" | "common" | "epicene" | null,
"english": string,
"literal": string | null,
"part_of_speech": integer,
"theme": integer,
"difficulty": integer,
"note": string | null,
"example": string | null,
"example_translation": string | null,
"vulgar": 1 | null,
"loan_word": 1 | null
}

FIELD LIMITS

spanish <= 128 chars
english <= 128 chars
literal <= 128 chars
note <= 512 chars
example <= 512 chars
example_translation <= 512 chars

FIELD RULES

spanish

* Boricua Spanish only.
* Store only the word or phrase.
* Normal sentence casing.
* Preserve accents.
* No punctuation unless naturally part of the phrase.

GENDERED NOUN FORMATTING

When a noun can naturally refer to either a male or female person AND Spanish uses different masculine/feminine forms, represent BOTH forms in `spanish`.

Masculine -o / feminine -a:

* Use `masculine/a`
* Examples: `niño/a`, `maestro/a`, `amigo/a`, `empleado/a`, `cajero/a`
* Do NOT use `(a)` for words ending in `-o`.

Masculine consonant + feminine -a:

* Use `masculine(a)`
* Examples: `trabajador(a)`, `profesor(a)`, `vendedor(a)`, `autor(a)`, `director(a)`

Irregular/substantially different forms:

* Use actual forms separated by `/` with NO spaces.
* Examples: `actor/actriz`, `rey/reina`, `héroe/heroína`
* Never invent forms such as `actor(a)`, `rey(a)`, `héroe(a)`, `actor/actora`, `rey/reya`.

Gender-specific nouns remain separate:

* `hombre`, `padre`, `esposo` → masculine
* `mujer`, `madre`, `esposa` → feminine

Do NOT combine distinct lexical entries such as `rey = King` and `reina = Queen` into one entry.

COMMON VS. EPICENE NOUNS

`common`: form changes according to the sex of the person.

* `niño/a`
* `trabajador(a)`
* `profesor(a)`

`epicene`: one fixed grammatical gender can refer to either sex.

* `persona` → F
* `víctima` → F
* `testigo` → M
* `miembro` → M

Do NOT use `U` for epicene nouns.

gender

* Populate ONLY when `part_of_speech = 1`.
* `M`, `F`, or `U`.
* Common nouns represented with both forms → `U`.
* Epicene nouns → grammatical gender (`M` or `F`).
* Otherwise `null`.

gender_type

* Populate ONLY when `part_of_speech = 1`.
* `regular`: fixed grammatical gender, not epicene.
* `common`: masculine/feminine forms can refer to either sex.
* `epicene`: fixed grammatical gender can refer to either sex.
* Otherwise `null`.

ADJECTIVE GENDER FORMATTING

Only modify `spanish`.

* Masculine `-o` / feminine `-a` → `masculine/a`

  * `alto/a`, `bajo/a`, `bonito/a`
* Masculine consonant + feminine `-a` → `masculine(a)`

  * `trabajador(a)`, `español(a)`
* Distinct forms → `masculine/feminine` with NO spaces.
* Invariable adjectives remain unchanged.

  * `grande`, `feliz`, `amable`, `interesante`, `inteligente`
* Never determine formatting from spelling alone.
* `gender` and `gender_type` MUST always be `null` for adjectives.

OTHER PARTS OF SPEECH

Do not apply noun/adjective gender formatting to verbs, adverbs, phrases, idioms, interjections, prepositions, or conjunctions.

part_of_speech

1 noun
2 verb
3 adjective
4 adverb
5 phrase
6 idiom
7 interjection
8 preposition
9 conjunction

Choose exactly ONE.

english

* Natural English equivalent.
* Sentence casing.
* Boricua meaning only.
* If multiple translations are ESSENTIAL, join with `/`.
* Maximum 4 translations.
* Example: `Bus / SUV / Truck / Van`

literal

* Populate ONLY when significantly different from the natural translation.
* Otherwise `null`.

theme

Choose exactly ONE:
1 everyday
2 home
3 food
4 family
5 people
6 emotions
7 health
8 clothing
9 work
10 tech
11 vehicles
12 travel
13 shopping
14 nature
15 animals
16 sports
17 government
18 religion
19 culture
20 pr_slang
21 pr_food
22 pr_culture
23 pr_places
24 pr_everyday

Prefer MOST SPECIFIC.

Examples:

* Mofongo → 21
* Jíbaro → 22
* Vieques → 23
* Guagua → 24
* Vacilón → 20

difficulty

For an English-speaking adult learning Puerto Rican Spanish:
1 Beginner
2 Intermediate
3 Advanced
4 Native-Level / Rare

note
Populate ONLY if useful:

* regional usage
* grammar clarification
* cultural explanation
* translation distinctions
  Otherwise `null`.

example
Generate ONLY when confidence is high.

* Natural
* Concise
* Demonstrates meaning
* Preferably 5–15 words
  Otherwise `null`.

example_translation
Translate `example`.
If `example = null`, then `example_translation = null`.

vulgar

* `1` if vulgar/offensive.
* Otherwise `null`.

loan_word

* `1` ONLY if borrowed from English.
* Examples: `Handle`, `Parking`, `Sandwich`
* Otherwise `null`.

GLOBAL RULES

* Preserve input order.
* Insert additional senses immediately after their source row.
* Trim whitespace.
* Collapse duplicate spaces.
* Preserve accents.
* Never invent meanings.
* Never assume Puerto Rican usage unless known.
* Prefer `null` over speculation.
* Do not create gender variants when Spanish does not actually use them.
* Do not combine distinct lexical entries merely because they are masculine/feminine counterparts.
* Carefully distinguish `regular`, `common`, and `epicene`.
* For adjectives, gender formatting affects ONLY `spanish`.
* Never change `gender` or `gender_type` for adjectives.
* Never put spaces around `/` when representing gender forms.
* Use `-o/a` when masculine ends in `-o` and feminine ends in `-a`.
* Use `(a)` when feminine is formed by adding `a` to a consonant-ending masculine form.
* Use `masculine/feminine` for genuinely distinct or irregular forms.
* Do not use `(a)` for words ending in `-o`.
* Do not alter an already-correct gender representation.
* If a word is invariable for gender, do not create a gender variant.

EXAMPLE

Input:
[
{
"id": "asdf",
"es": "Malla",
"en": null
}
]

Possible output:
[
{
"sourceId": "asdf",
"rowId": "asdf_1",
"record": {
"spanish": "Malla",
"gender": "F",
"gender_type": "regular",
"english": "Mesh / Net",
"literal": null,
"part_of_speech": 1,
"theme": 1,
"difficulty": 2,
"note": null,
"example": null,
"example_translation": null,
"vulgar": null,
"loan_word": null
},
"existingReferences": null
},
{
"sourceId": "asdf",
"rowId": "asdf_2",
"record": {
"spanish": "Malla",
"gender": "F",
"gender_type": "regular",
"english": "Swimsuit",
"literal": null,
"part_of_speech": 1,
"theme": 8,
"difficulty": 2,
"note": null,
"example": null,
"example_translation": null,
"vulgar": null,
"loan_word": null
},
"existingReferences": null
}
]

QUALITY CHECK

Before finalizing:

1. Every input element produced at least one output object.
2. Every output object has exactly `sourceId`, `rowId`, `record`, `existingReferences`.
3. `sourceId` exactly matches the input `id`.
4. `rowId` uses the correct 1-based sense number.
5. `record` contains exactly the 13 database fields.
6. `existingReferences` is always `null`.
7. All enum values are valid.
8. JSON is valid.
9. Output order is preserved.
10. Additional meanings are inserted immediately after their source.
11. Every noun has the correct `gender`/`gender_type` relationship.
12. Every common noun has `gender = "U"`.
13. Every epicene noun has `gender = "M"` or `"F"`, never `"U"`.
14. Every adjective has `gender = null` and `gender_type = null`.
15. Gender formatting follows all noun/adjective rules.
16. No distinct lexical masculine/feminine entries are improperly combined.

OUTPUT CONTRACT

* Output EXACTLY one JSON array.
* JSON only.
* No markdown.
* No comments.
* No code fences.
* Use double quotes.
* Escape characters properly.
* No trailing commas.
* Must parse without modification.
* First character must be `[`.
* Last character must be `]`.

INPUT JSON ARRAY:

<INPUT>